server:
  port: 8051
  servlet:
    context-path: /auth_user

spring:
  profiles:
    active: dev
  output:
    ansi:
      enabled: always

logging:
  level:
    com.ead: TRACE
    root: info
    org.springframework.web: debug
    org.hibernate: info